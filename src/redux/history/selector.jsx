export const historyMVSelector = (state) => state.history.mv;
export const historySongSelector = (state) => state.history.song;
export const historyPlaylistSelector = (state) => state.history.playlist;
