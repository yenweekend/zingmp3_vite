export const collectionSelector = (state) => state.collection;
export const playlistCollectionSelector = (state) => state.collection.playlist;
export const songCollectionSelector = (state) => state.collection.song;
